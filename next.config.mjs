/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['res.cloudinary.com','via.placeholder.com'], // Add other domains as needed
    },
};

export default nextConfig;
